# 학습 표본·변환

연속 목표월과 거시 입력의 대응, tcode·결측·표준화·PCA를 소유한다. 군집·배분·다운로드는 소유하지 않는다.

차분은 연속 달력에서 계산하고 적합 통계는 허용 학습 표본에서만 얻는다. 미래의 결측이나 값으로 과거 변수 선택이 바뀌면 안 된다.

미래 교란, 48개월 연속성, 모든 tcode, 결측 경계와 PCA rank를 검사한다.
