# 연구 패키지

공통 설정·오류·식별자·CLI와 역할별 계산 모듈을 소유한다. 역사 audit_tests나 legacy를 import하지 않는다. import만으로 외부 요청·학습·출력 생성이 일어나면 안 된다.

하위 계산은 입력으로 받은 자료를 처리하고 CLI/실행 계층이 조립한다. 데이터셋·특징·분할 ID를 이름이나 행 개수만으로 대체하지 않는다. 설정 변경은 strict 파서, 종료코드, 해석된 경로를 함께 확인한다. 공통 변경 후 tests 전체를 최종 통합 상태에서 한 번 실행한다.
