# Preserve generated/copied evidence bytes and manifest hashes on Windows checkouts.
# Whitespace normalization would invalidate these immutable source copies.
reports/reproduction/** -text -whitespace
